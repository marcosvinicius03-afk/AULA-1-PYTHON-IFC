# 6. Criar um algoritmo para ler a base e a altura de um triângulo e mostrar a sua área ( ( base x altura ) / 2 ).

# Entrada
base = float(input("valor da base: "))
altura = float(input("valor da altura: "))

# Processamento
area = ((base*altura)/2)

# Saida
print(f"A area vale {area:.2f}")